export interface TestimonialsProps {
  // Future props can be added here
}

export interface Testimonial {
  name: string;
  role: string;
  company: string;
  rating: number;
  text: string;
  avatar: string;
}